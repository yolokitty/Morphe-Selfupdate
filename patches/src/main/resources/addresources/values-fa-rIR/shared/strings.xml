<?xml version="1.0" encoding="utf-8"?>
<!--

    Strings with new lines must be raw strings where they're wrapped in quotes and new lines are not encoded.
    Raw strings still require escaping embedded double quotes but escaping embedded single quotes is optional.

    Raw strings are required because Crowdin AI translations regularly gets confused and
    replace \n with an encoded new line character.

    Bad:
        <string name="summary_key">First \'item\' text\nSecond \"item\" text</string>

    Good:
        <string name="summary_key">"First 'item' text
Second \"item\" text"</string>

-->
<resources>
  <!-- misc.settings.settingsResourcePatch -->
  <string name="morphe_settings_submenu_title">تنظیمات</string>
  <!-- String is also duplicated in Reddit import / export preference -->
  <!-- String is also duplicated in Reddit about preference -->
  <!-- misc.debugging.enableDebuggingPatch -->
  <!-- Keep short, four of these are shown in one row. -->
  <!-- Keep short, four of these are shown in one row. -->
  <!-- Keep short, four of these are shown in one row. -->
  <!-- Keep short, four of these are shown in one row. -->
  <!-- Keep short, shown as a badge and as a button. -->
  <!-- Keep short, shown as a badge and as a button. -->
  <string name="morphe_debug_feature_flags_manager_state_blocked">خاموش</string>
  <!-- Keep short, shown as a badge and as a button. -->
  <string name="morphe_debug_feature_flags_manager_state_forced">روشن</string>
  <string name="morphe_debug_feature_flags_manager_action_remove">حذف</string>
  <!-- Could be its own resource file, but for simplicity it's shared among all apps. -->
  <!-- misc.gms.gmsCoreSupportResourcePatch -->
  <!-- Translations of this should not be longer than the original English text, otherwise the text can be clipped and not entirely shown. -->
  <!-- "Don't kill my app" should be kept in English and not translated -->
  <!-- misc.gms.linkHandlingPatch -->
  <!-- misc.checks.experimentalAppNoticePatch -->
</resources>
